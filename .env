# Database Configuration - CHANGE FOR PRODUCTION
# For production: Use managed PostgreSQL/MySQL service
DATABASE_URL="file:./prisma/dev.db"

# JWT Secret Key - MUST BE CHANGED IN PRODUCTION
# This is a demo value - replace with: openssl rand -base64 32
# Never commit real secrets to version control
JWT_SECRET="change-this-secret-in-production-with-strong-random-string"

# Application Environment
NODE_ENV="production"

# API Configuration - CHANGE FOR PRODUCTION DOMAIN
NEXT_PUBLIC_API_URL="https://yourdomain.com"

# Security Settings
CORS_ORIGIN="https://yourdomain.com"

# Feature Flags
NEXT_PUBLIC_ENABLE_AUDIT_LOG="true"
NEXT_PUBLIC_ENABLE_BACKUPS="true"